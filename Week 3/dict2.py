aDict = {'a': ['aardvark'], 'c': ['coati'], 'b': ['baboon'], 'd': ['donkey', 'dog', 'dingo']}
def biggest(aDict):
    big = 0
    if
    for e in aDict:
        if(len(aDict[e]) > big):
            big = len(aDict[e])
            KEY = e
            #print big,KEY
    return KEY
